# Allow all search engines to index the site
User-agent: *
Allow: /

# Sitemap location
Sitemap: https://askharekrishna.com/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1

# Disallow admin or sensitive routes if any
# Disallow: /admin/
# Disallow: /api/
