import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ArthaPathaPage } from './artha-patha.page';

describe('ArthaPathaPage', () => {
  let component: ArthaPathaPage;
  let fixture: ComponentFixture<ArthaPathaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ArthaPathaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
