import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpadeshamritamPage } from './upadeshamritam.page';

describe('UpadeshamritamPage', () => {
  let component: UpadeshamritamPage;
  let fixture: ComponentFixture<UpadeshamritamPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpadeshamritamPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
