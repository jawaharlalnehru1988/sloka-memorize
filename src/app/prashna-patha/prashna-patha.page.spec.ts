import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PrashnaPathaPage } from './prashna-patha.page';

describe('PrashnaPathaPage', () => {
  let component: PrashnaPathaPage;
  let fixture: ComponentFixture<PrashnaPathaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PrashnaPathaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
